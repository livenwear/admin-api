export enum TicketStatus {
  OPEN = 'OPEN',
  PENDING = 'PENDING',
  ANSWERED = 'ANSWERED',
  CLOSED = 'CLOSED',
}

export enum TicketPriority {
  LOW = 'LOW',
  NORMAL = 'NORMAL',
  HIGH = 'HIGH',
  URGENT = 'URGENT',
}

export enum TicketCategory {
  ORDER = 'ORDER',
  PAYMENT = 'PAYMENT',
  SHIPPING = 'SHIPPING',
  RETURN = 'RETURN',
  PRODUCT = 'PRODUCT',
  ACCOUNT = 'ACCOUNT',
  OTHER = 'OTHER',
}

import { AbstractEntity } from '../../common/abstract.entity';
import { Product } from './product.entity';
export declare class ProductTag extends AbstractEntity {
    name: string;
    slug: string;
    description: string;
    colorCode: string;
    displayOrder: number;
    isActive: boolean;
    metadata: Record<string, any>;
    products: Product[];
}
